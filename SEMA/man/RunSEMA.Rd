% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSEMA.R
\name{RunSEMA}
\alias{RunSEMA}
\title{Run SEMA analysis}
\usage{
RunSEMA(
  object,
  gene_sets,
  assay = NULL,
  slot = "data",
  coords = NULL,
  reduction = "spatial",
  k = 30,
  raw_method = "mean",
  spatial_weight = 0.5,
  bandwidth = NULL,
  addmodulescore_params = list(),
  verbose = TRUE,
  n_cores = 1,
  ...
)
}
\arguments{
\item{object}{Seurat object or expression matrix}

\item{gene_sets}{Gene set list or single gene set vector}

\item{assay}{Assay name to use (for Seurat objects only), default "RNA"}

\item{slot}{Slot to use (for Seurat objects only), default "data"}

\item{coords}{Coordinate matrix, if Seurat object and not provided, obtained from reductions$spatial}

\item{reduction}{Dimensionality reduction method to obtain coordinates (for Seurat objects only), default "spatial"}

\item{k}{Number of nearest neighbors, default 30}

\item{raw_method}{Raw score calculation method: "mean" (default) or "addmodulescore"}

\item{spatial_weight}{Spatial smoothing weight, between 0-1, default 0.5}

\item{bandwidth}{Gaussian kernel bandwidth, automatically estimated if NULL}

\item{addmodulescore_params}{AddModuleScore parameter list}

\item{verbose}{Whether to display detailed information, default TRUE}

\item{n_cores}{Number of parallel cores, default 1}

\item{...}{Other parameters passed to CalculateSEMA or CalculateMultipleSEMAs}
}
\value{
SEMA result object
}
\description{
Spatially aware gene set expression analysis, supports Seurat object input
}
\examples{
\dontrun{
# Using Seurat object
library(Seurat)
data("pbmc_small")

# Create simulated spatial coordinates
coords <- matrix(runif(ncol(pbmc_small)*2), ncol=2)
rownames(coords) <- colnames(pbmc_small)

# Add coordinates to Seurat object
pbmc_small[["spatial"]] <- CreateDimReducObject(
  embeddings = coords,
  key = "spatial_",
  assay = "RNA"
)

# Define gene sets
gene_sets <- list(
  cell_cycle = c("MCM5", "PCNA", "TYMS"),
  stress_response = c("HSPA1A", "HSPA1B", "DNAJB1")
)

# Run SEMA
result <- RunSEMA(pbmc_small, gene_sets = gene_sets)
}

}
